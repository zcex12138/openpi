# 项目概览

- openpi 是 Physical Intelligence 的开源机器人 VLA 模型仓库，提供 pi0 / pi0-FAST / pi0.5 等模型、预训练与微调权重，以及训练、推理、策略服务与远程推理客户端。
- 主要语言：Python 3.11。核心包在 `src/openpi/`，脚本入口在 `scripts/`，远程推理客户端在 `packages/openpi-client/`。
- 示例与平台适配流程在 `examples/`，文档在 `docs/`。
- 主要数据/平台：DROID、ALOHA、LIBERO。